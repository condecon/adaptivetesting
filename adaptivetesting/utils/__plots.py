import matplotlib.pyplot as plt
from matplotlib.figure import Figure
from matplotlib.axes import Axes

def plot_final_ability_estimates() -> tuple[Figure, Axes]:
    fig, ax = plt.subplots()

    return (fig, ax)